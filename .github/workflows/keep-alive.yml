name: Keep Supabase Awake
 
on:
  schedule:
    # Runs at 1:00 PM UTC every Monday and Thursday
    - cron: '0 13 * * 1,4'
  workflow_dispatch: # lets you also trigger it manually from the Actions tab
 
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          curl -s -o /dev/null -w "%{http_code}" \
            "https://iwxlyeaxlkqmjottaxkx.supabase.co/rest/v1/passwords?select=id&limit=1" \
            -H "apikey: sb_publishable_WoR_OZhb_3tMTRJhPeozVg_LwLp-73a" \
            -H "Authorization: Bearer sb_publishable_WoR_OZhb_3tMTRJhPeozVg_LwLp-73a"
